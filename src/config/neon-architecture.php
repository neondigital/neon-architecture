<?php

return [
    // Should we add "Action" to the end of generated actions?
    'suffix_actions' => true,

    // Should we add "Process" to the end of generated processes?
    'suffix_processes' => true,
];
